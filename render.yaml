services:
  - type: web
    name: aurastadium-2026
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 3000
      - key: NODE_VERSION
        value: 18
    plan: free
